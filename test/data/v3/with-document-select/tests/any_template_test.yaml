suite: Document Selector to match any template
templates:
- "*.yaml"
tests:
  - it: should match the correct kind when top templates have greedy pattern matching.
    templates:
      - templates/foo-deployment.yaml
    documentSelector:
      path: kind
      value: Deployment
    asserts:
      - equal:
          path: kind
          value: Deployment
      - hasDocuments:
          count: 1
