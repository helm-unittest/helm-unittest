suite: Document Selector test, templates outside the tets block
templates:
  - templates/deployment.yaml
tests:
  - it: selects the correct kind
    documentSelector:
      path: kind
      value: Deployment
    asserts:
      - equal:
          path: kind
          value: Deployment
      - equal:
          path: metadata.name
          value: foo-deployment
