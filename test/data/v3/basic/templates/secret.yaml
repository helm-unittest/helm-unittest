apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
stringData:
  base-data.sh: "command"
  password: {{ .Values.secret.password }}
  passwordbase64: {{ .Values.base64.password }}
