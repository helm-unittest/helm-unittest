suite: Configmap mulit line Test
templates:
  - configmap.yaml
tests:
  - it: should NOT configure ssl params if NOT set to be exposed
    asserts:
      - matchRegex:
          path: data.my\.conf
          pattern: cacertfile