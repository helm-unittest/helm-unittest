suite: test suite name
files:
  - a.yaml
  - b.yaml
tests:
  - it: should ...
    values:
      - values.yaml
    set:
      a.b.c: ABC
      x.y.z: XYZ
    asserts:
      # - matchSnapshot:
      - equal:
          path:
          value:
      - notEqual:
      - matchRegex:
          path:
          pattern:
      - notMatchRegex:
      - contains:
      - notContains:
      - isNull:
      - isNotNull:
      - empty:
      - isNotEmpty:
      - isKind:
      - isAPIVersion:
      - hasDocuments:
