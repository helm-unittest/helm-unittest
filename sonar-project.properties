sonar.projectKey=helm-unittest_helm-unittest
sonar.organization=helm-unittest

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=helm-unittest
sonar.projectVersion=0.8.1

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Define all sources and exclude test-, and vendor files.
sonar.sources=.
sonar.exclusions=**/*_test.go,**/vendor/**,**/test/data/**,**/test-results/**,**/testdata/**

# Define all test sources and exclude vendor file from testing.
sonar.tests=.
sonar.test.inclusions=**/*_test.go
sonar.test.exclusions=**/vendor/**

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Setup code coverage report path
sonar.go.coverage.reportPaths=test-results/coverage
